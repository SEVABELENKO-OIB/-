факт определения(n):

    0 < n if:
        возвращает 0
    ещё:
        s = 1
        диапазон в i для(1,n+1):
            s *= i
        возвращает s
int = b(input('Введите число: '))
= список []

диапазон в i для(факт(b),0,-1):
    список.добавить(факт(я))

печать(список)