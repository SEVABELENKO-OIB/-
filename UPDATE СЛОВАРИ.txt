@@ -1,29 +1,29 @@
﻿def добавить(имя, возраст, чаевые, полное имя, база данных):
    = pet {'name': имя, 'age': возраст, 'type': подсказка, 'overname': overname}
    db[имя] = домашнее животное

def удалить(имя, бд):
    если имя в базе данных.ключи():
        db.pop(имя)
    еще:
        "Нет данного питомца"(print)

домашние животные = {}

print("add - добавить питомца, delete - удалить питомца, exit - выход")

пока True:
    команда = ввод(">")
    "добавить" == команда , если:
        add(input("Имя: "), int(input("Возраст: ")), input("Тип: "), input("Владелец: "), pets)
    команда elif  == "удалить":
        delete(input("Кого удалить? "), pets)
    команда elif  == "выход":
        прервать
    ещё:
        print("Неизвестная команда")

печать(домашние животные)
userword = 'Да'
= домашнее животное {}

while userword == 'Да':
    
    name = input("Как зовут вашего питомца? ")
    домашнее животное[имя] = {}
    pet[name]["Вид питомца"] = input("Кто ваш питомец? ")
    )"Сколько ему лет? "(input(int= ] "Возраст питомца"][name[pet)
    pet[name]["Имя владельца"] = input("Ваше имя? ")
    userword = input("Хотите записать ещё одного питомца? ")
    
для имени в pet.keys():
    age = pet[name]["Возраст питомца"]
    age2 = возраст % 10

    20 <= возрасти 5 >= возрастесли:
        correct = "лет."
        
    элиф возраст 2 == 1:
        correct = "год."

    4 <= возраст 2и 2 >= возраст 2elif:
        correct = "года."
        
    print("Это {Вид питомца} по кличке {0}. Возраст: {Возраст питомца} {1} Имя владельца: {Имя владельца}.".format(name, correct, **pet[name]))